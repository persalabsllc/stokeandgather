import { categories, previewProducts } from '../lib/catalog';
export default function sitemap() { const base = process.env.NEXT_PUBLIC_SITE_URL || 'https://www.stokeandgather.com'; const staticRoutes = ['', '/pages/about', '/pages/shipping-returns', '/pages/contact', '/pages/privacy', '/pages/terms']; return [...staticRoutes.map((route) => ({ url: `${base}${route}`, changeFrequency: 'monthly', priority: route ? 0.6 : 1 })), ...categories.map((category) => ({ url: `${base}/collections/${category.slug}`, changeFrequency: 'weekly', priority: 0.8 })), ...previewProducts.map((product) => ({ url: `${base}/products/${product.handle}`, changeFrequency: 'weekly', priority: 0.7 }))]; }
